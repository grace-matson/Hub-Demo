name: build

on:
  workflow_run:
    types:
      - requested
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Echo
      run: |
        echo "${{ github.event.workflow_run}}"